# C 内存管理

这一页作为旧入口兼容。C 入门阶段的内存理解主要分三层：

1. 局部变量通常在函数调用期间存在。
2. 数组是一段连续空间，访问时要保证下标合法。
3. 指针保存地址，字符串依赖 `'\0'` 作为结束标记。

推荐先阅读：[指针与字符串](./pointers-strings.md)。

后续如果扩展动态内存，再补充 `malloc`、`free`、内存泄漏、重复释放和悬空指针。
